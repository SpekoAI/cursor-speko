---
description: How to reach Speko correctly — which host answers which surface, and how the one Speko key is used. Apply whenever the work touches speech, transcription, model routing, voice agents or phone calls through Speko.
alwaysApply: false
---

# Working with Speko

Speko is two hosts and one key. Pick the host by the surface you need; the same `sk_live_`
credential authenticates both.

| Host | What it serves | Base URL |
| --- | --- | --- |
| Router | STT, LLM, TTS — one request, benchmark-routed | `https://api.speko.ai/v1` |
| Platform | Agents, sessions, phone numbers, calls, knowledge bases, evals | `https://api.speko.dev/v1` |

One key, minted once at [platform.speko.ai/api-keys](https://platform.speko.ai/api-keys), works
on both. Keys are environment-scoped, so a staging key still fails against production — that is
the 401 to check for first. The hosted MCP at `mcp.speko.ai` speaks the platform side and takes
OAuth or the same key.

## Rules

- Prefer the `speko` skill for transcription, synthesis and routing; prefer the Speko MCP
  tools for anything that creates, deploys, inspects or bills an agent.
- Never hardcode a vendor. `model: "auto"` routes on measured benchmarks; pin a model only
  when the user names one, and only from a `routable: true` row of `GET /v1/models`.
- Justify a routing choice with `GET /v1/routing/preview` before spending. It costs nothing
  and returns the pick, the reason, and the benchmark window behind it.
- Always request `response_format: "wav"` from `/v1/audio/speech`. The default is headerless
  24 kHz PCM that no player opens, and mp3 is refused before any upstream call.
- Read the `x-route` response header rather than assuming a pin won.
- Placing a phone call is irreversible and billed. Confirm the E.164 number, the purpose and
  the local hour with the user before dialing, every time. See the `speko-calls` skill.
- Read keys from the environment. Never inline one into a command that gets logged.
- `speko-calls` reads `SPEKO_PLATFORM_API_KEY`. That is the same Speko key under a second name,
  set deliberately and only where dialing is approved — the separation is a safety gate, not a
  technical requirement.
